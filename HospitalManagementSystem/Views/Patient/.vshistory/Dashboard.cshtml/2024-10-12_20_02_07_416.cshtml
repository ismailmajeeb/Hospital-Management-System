@model PatientDashBoardModel

<title>Patient Dashboard</title>

<!-- Bootstrap 5 CSS -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<!-- Bootstrap Icons -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.8.1/font/bootstrap-icons.min.css" rel="stylesheet">

<style>
    .card {
        border: none;
        border-radius: 12px;
        background-color: #fff;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .card-title {
        font-weight: bold;
        color: #007bff; /* Matching title color */
    }

    .btn-custom {
        background-color: #007bff;
        color: white;
        border-radius: 50px;
    }

        .btn-custom:hover {
            background-color: #0056b3; /* Darker shade for hover effect */
        }

    .icon-circle {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background-color: #007bff;
        color: white;
        font-size: 24px;
        margin-bottom: 15px;
    }

    .section-title {
        margin: 2rem 0 1rem;
        font-weight: bold;
        color: #007bff;
        border-bottom: 2px solid #007bff;
        display: inline-block;
        padding-bottom: 0.5rem;
    }
</style>

<div class="container my-5">
    <h2 class="text-center mb-5">Patient Dashboard</h2>

    <!-- Patient Information -->
    <div class="row mb-4">
        <div class="col-md-4">
            <div class="card p-4 text-center">
                <div class="icon-circle">
                    <i class="bi bi-person"></i>
                </div>
                <h5 class="card-title">Patient Information</h5>
                <ul class="list-unstyled mb-0">
                    <li><strong>Name:</strong> @Model.Name</li>
                    <li><strong>Age:</strong> @Model.Age</li>
                    <li><strong>Gender:</strong> @Model.Gender</li>
                    <li><strong>Blood Group:</strong> @Model.BloodGroup</li>
                </ul>
            </div>
        </div>

        <!-- Health Overview -->
        <div class="col-md-4">
            <div class="card p-4 text-center">
                <div class="icon-circle">
                    <i class="bi bi-heart-pulse"></i>
                </div>
                <h5 class="card-title">Health Overview</h5>
                <ul class="list-unstyled mb-0">
                    <li><strong>Chronic Conditions:</strong>@Model.ChronicConditions</li>
                    <li><strong>Allergies:</strong>@Model.Allergies</li>
                </ul>
                <!-- Button to view all medical records -->
                <form asp-action="MedicalRecords" asp-controller="Patient" method="post">
                    <input type="submit" class="btn btn-custom mt-3" value="View All Medical Records"></input>
                </form>

            </div>
        </div>

        <!-- Appointments Section -->
        <div class="col-md-4">
            <div class="card p-4 text-center">
                <div class="icon-circle">
                    <i class="bi bi-calendar-event"></i>
                </div>
                <h5 class="card-title">Next Appointment</h5>
                <ul class="list-unstyled mb-0">
                    <li><strong>Date:</strong> @Model.NextAppointmentDateTime?.ToShortDateString()</li>
                    <li><strong>Doctor:</strong> @Model.DoctorName</li>
                </ul>
                <form asp-action="Appointments" asp-controller="Patient" method="post">
                    <input type="submit" class="btn btn-custom mt-3" value="View All Appointments"></input>
                </form>
            </div>
        </div>
    </div>

  
</div>

<!-- Bootstrap 5 JS and Popper.js -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
