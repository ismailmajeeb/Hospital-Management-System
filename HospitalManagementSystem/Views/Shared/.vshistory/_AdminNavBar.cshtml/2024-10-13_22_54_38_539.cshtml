<!-- Bootstrap Icons -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.8.1/font/bootstrap-icons.min.css" rel="stylesheet">

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <!-- Navbar brand (Logo or Title) -->
        <a class="navbar-brand" href=@Url.Action("index","Home")>
            <i class="bi bi-hospital"></i> Hospital Admin
        </a>

        <!-- Hamburger Menu for mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar content -->
        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <!-- Dashboard -->
                <li class="nav-item">
                    <a href=@Url.Action("DashBoard","Admin") class="nav-link active" aria-current="page">
                        <i class="bi bi-grid"></i> Dashboard
                    </a>
                </li>
                <!-- Patients -->
                <li class="nav-item">
                    <a class="nav-link" href=@Url.Action("Patients","Admin")>
                        <i class="fas fa-user-injured"></i> Patients
                    </a>
                </li>
                <!-- Doctors -->
                <li class="nav-item">
                    <a class="nav-link" href=@Url.Action("Doctors","Admin")>
                        <i class="fas fa-user-md"></i> Doctors
                    </a>
                </li>
                <!-- Nurses -->
                <li class="nav-item">
                    <a class="nav-link" href=@Url.Action("Nurses","Admin")>
                        <i class="fas fa-user-nurse"></i> Nurses
                    </a>
                </li>
                <!-- Appointments -->
                <li class="nav-item">
                    <a class="nav-link" href=@Url.Action("Appointments","Admin")>
                        <i class="fas fa-calendar-alt"></i> Appointments
                    </a>
                </li>
            </ul>

            <!-- Right-side options (Profile and Logout) -->
            <ul class="navbar-nav">
                <!-- Profile -->
                <li class="nav-item">
                    <a class="nav-link" href=@Url.Action("Profile","Admin")>
                        <i class="bi bi-person-circle"></i> Profile
                    </a>
                </li>
                <!-- Logout -->
                <li class="nav-item">
                    <a class="nav-link text-danger" href=@Url.Action("LogOff","Account")>
                        <i class="bi bi-box-arrow-right"></i> Logout
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<!-- Bootstrap 5 JS and Popper.js -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<style>
/* Customize Navbar Hover Effects */
    /* Link hover effect */
    .nav-link:hover {
        background-color: rgba(0, 0, 0, 0.05); /* Subtle hover background */
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }

    /* Active link customization */
    .nav-link.active {
        font-weight: bold;
        color: #007bff; /* Blue to indicate active state */
    }

    /* Navbar brand hover effect */
    .navbar-brand:hover {
        color: #0056b3; /* Darker blue for brand hover effect */
        transition: color 0.3s ease;
    }
</style>